# deps & builds
node_modules/
.next/
dist/
*.tsbuildinfo

# local data (embedded PGlite database)
.data/

# env & secrets — env.example (no leading dot) is the committed template
.env
.env.*

# misc
.DS_Store
.vercel/
.claude/settings.local.json
